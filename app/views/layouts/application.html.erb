<!DOCTYPE html>
<html>
  <head>
    <title>Favolist40</title>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%# because importing dropzone in the application.scss file doesn't seem to work %>
    <script src="https://unpkg.com/dropzone@6.0.0-beta.1/dist/dropzone-min.js"></script>
    <link href="https://unpkg.com/dropzone@6.0.0-beta.1/dist/dropzone.css" rel="stylesheet" type="text/css" />

    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <%= render "shared/flashes" %>
    <% unless controller_name == 'sessions' && action_name == 'new' %>

      <div class="desktop_grid">
        <%= render "shared/navbar_desktop" %>
        <div class="<%= (controller_name == 'pages' && (action_name == 'about' || action_name == 'beta')) || controller_name == 'settings' ? 'main-section-desktop-content-page' : 'main-section-desktop' %>">
          <%= yield %>
        </div>
        <% unless controller_name == 'pages' && (action_name == 'about' || action_name == 'beta') || controller_name == 'settings' %>
          <%= render "shared/sidebar" %>
        <% end %>
      </div>
      <%= render "shared/navbar" %>
    <% else %>
      <%= yield %>
    <% end %>
  </body>
</html>
