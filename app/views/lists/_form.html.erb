<h3>Share a favorite product</h3>
<div class="container">
  <%= simple_form_for @list do |f| %>
    <%= f.input :name, required: true %>
    <%= f.input :description %>
    <%= f.button :submit %>
  <% end %>
</div>
